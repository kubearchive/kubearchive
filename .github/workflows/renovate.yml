# Copyright Kronicler Authors
# SPDX-License-Identifier: Apache-2.0
#
# Renovate: https://github.com/renovatebot/github-action
# Action based on: https://github.com/renovatebot/github-action?tab=readme-ov-file#example-with-github-app
---
name: Renovate
on:
  workflow_dispatch:
  schedule:
    - cron: "15 * * * *"  # Run hourly at 15 minutes past the hour

jobs:
  setup:
    uses: ./.github/workflows/log_run_details.yml
  renovate:
    if: github.repository == 'kronicler/kronicler'  # Don't run renovate on forks
    runs-on: ubuntu-latest
    steps:
      - name: Get kronicler-renovate Github App Api Token
        id: get_token
        uses: actions/create-github-app-token@v1
        with:
          private-key: ${{ secrets.KRONICLER_RENOVATE_PRIVATE_KEY }}
          app-id: ${{ secrets.KRONICLER_RENOVATE_APP_ID }}

      - name: Checkout Kronicler repository
        uses: actions/checkout@v4

      - name: Run Renovate
        uses: renovatebot/github-action@v41.0.17
        with:
          token: '${{ steps.get_token.outputs.token }}'
        env:
          # run renovate on this repo
          RENOVATE_REPOSITORIES: ${{ github.repository }}
          # renovate will generate an onboarding PR after the first run
          RENOVATE_ONBOARDING: "true"
          # username needed for Github auth (Github app name + "[bot]")
          RENOVATE_USERNAME: "kronicler-renovate[bot]"
          # git commit author. Format is: Github app name <id+RENOVATE_USERNAME@users.noreply.github.com>
          # id can be retrieved from https://api.github.com/users/kronicler-renovate[bot]
          RENOVATE_GIT_AUTHOR: "kronicler-renovate <171263778+kronicler-renovate[bot]@users.noreply.github.com>"
          # renovate will create signed commits
          RENOVATE_PLATFORM_COMMIT: "true"
