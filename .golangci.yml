# Copyright KubeArchive Authors
# SPDX-License-Identifier: Apache-2.0
---
version: '2'
run:
  build-tags:
    - integration
linters:
  enable:
    - bodyclose
    - containedctx
    - fatcontext
    - gosec
    - nilerr
    - nilnil
    - unparam
    - usestdlibvars
    - wastedassign
  settings:
    staticcheck:
      checks:
        # defaut
        - all
        - -ST1000
        - -ST1003
        - -ST1016
        - -ST1020
        - -ST1021
        - -ST1022
        # /default
        - -QF1003 # use switch instead of if/ifelse/else
        - -ST1005 # do not capitalize errors
    govet:
      enable:
        - loopclosure
        - nilness
        - shadow
      enable-all: false
      disable-all: false
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
