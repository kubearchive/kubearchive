# Copyright Kronicler Authors
# SPDX-License-Identifier: Apache-2.0
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "kronicler-sink"
  namespace: kronicler
  labels:
    app.kubernetes.io/name: "kronicler-sink"
    app.kubernetes.io/component: sink
    app.kubernetes.io/part-of: kronicler
    app.kubernetes.io/version: "${NEXT_VERSION}"
spec:
  replicas: 1
  selector:
    matchLabels: &labels
      app: kronicler-sink
  template:
    metadata:
      labels: *labels
    spec:
      serviceAccountName: kronicler-sink
      volumes:
        - name: logging-config
          configMap:
            name: kronicler-logging
      containers:
        - name: kronicler-sink
          image: ko://github.com/kronicler/kronicler/cmd/sink
          resources:
            requests:
              cpu: 200m
              # 90% of the limit
              # See recommendations at https://tip.golang.org/doc/gc-guide#Memory_limit
              memory: 230Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - mountPath: /data/logging
              name: logging-config
          envFrom:
            # Provide DB URL, user, password, database and port as env vars
            - secretRef:
                name: kronicler-database-credentials
          env:
            - name: KRONICLER_ENABLE_PPROF
              value: "true"
            - name: GIN_MODE
              value: release
            - name: LOG_LEVEL
              value: INFO
            - name: KRONICLER_OTEL_MODE
              value: "disabled"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: ""
            - name: KRONICLER_OTLP_SEND_LOGS
              value: "false"
            - name: OTEL_GO_X_DEPRECATED_RUNTIME_METRICS
              value: "false"
            - name: GOMEMLIMIT
              valueFrom:
                resourceFieldRef:
                  resource: limits.memory
            - name: GOMAXPROCS
              valueFrom:
                resourceFieldRef:
                  resource: limits.cpu
            - name: KRONICLER_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: KRONICLER_LOGGING_DIR
              value: /data/logging
          ports:
            - containerPort: 8080
              name: sink
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /livez
              port: 8080
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
---
kind: Service
apiVersion: v1
metadata:
  name: "kronicler-sink"
  namespace: kronicler
  labels:
    app.kubernetes.io/name: "kronicler-sink"
    app.kubernetes.io/component: sink
    app.kubernetes.io/part-of: kronicler
    app.kubernetes.io/version: "${NEXT_VERSION}"
spec:
  selector:
    app: kronicler-sink
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
